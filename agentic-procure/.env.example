# AgenticProCure Environment Configuration
# Copy this file to .env and customize as needed

# Environment - determines which services to use
# Options: development, testing, production
NODE_ENV=development

# API Key for development/testing (simple authentication)
API_KEY=agentic-procure-dev-key-123

# AWS Configuration (only needed for production)
# AWS_REGION=us-east-1
# AWS_BEDROCK_MODEL=anthropic.claude-3-sonnet-20240229-v1:0

# Local Development Endpoints (optional)
# Uncomment if running local AWS services (localstack, etc.)
# DYNAMODB_ENDPOINT=http://localhost:8000
# S3_ENDPOINT=http://localhost:4566
# LAMBDA_ENDPOINT=http://localhost:4566

# Application Configuration
PORT=3001
FRONTEND_PORT=3000

# Mock Data Configuration (development only)
MOCK_RESPONSE_DELAY_MS=500
ENABLE_MOCK_LOGGING=true